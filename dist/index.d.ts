import type cytoscape from 'cytoscape';
/**
 * Registers the Dagre layout extension on a Cytoscape library reference.
 */
declare const register: (cy?: typeof cytoscape) => void;
export default register;
